/**
 * @file config_data.cpp
 * @brief Definición de la variable global del sistema
 * @details Instancia la variable global `sistema` que contiene todo el estado.
 */

#include "config_data.h"

/**
 * @brief Variable global con todo el estado del sistema
 * @details Acceso desde otros módulos: #include "../core/config_data.h" → usar `sistema`
 */
SistemaConfig sistema;